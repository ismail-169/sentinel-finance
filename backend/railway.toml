[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[build.nixpacks]
providers = ["python"]

[deploy]
startCommand = "python3 -m uvicorn api:app --host 0.0.0.0 --port ${PORT:-8080}"