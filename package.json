{
  "name": "mnee-sentinel",
  "version": "1.0.0",
  "description": "Sentinel Finance - AI-powered security for MNEE stablecoin",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network mainnet",
    "seed": "hardhat run scripts/seedVendors.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  }
}
